{
	"name": "cross-seed-workspace",
	"version": "0.0.0",
	"description": "Cross-seed monorepo workspace",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/cross-seed",
		"packages/shared",
		"packages/webui",
		"packages/api-types"
	],
	"scripts": {
		"test": "npm --workspaces run test --if-present",
		"coverage": "npm --workspaces run coverage --if-present",
		"build": "npm -w packages/cross-seed run build:all",
		"build:api-types": "npm -w packages/api-types run build",
		"build:webui": "npm -w packages/webui run build",
		"build:all": "npm run build",
		"watch": "npm -w packages/cross-seed run watch",
		"dev": "npm -w packages/cross-seed run build && concurrently --names \"TS,BE,FE\" --prefix-colors \"blue,green,magenta\" \"npm -w packages/cross-seed run watch\" \"node --watch packages/cross-seed/dist/cmd.js daemon\" \"npm -w packages/webui run dev\"",
		"dev:backend": "npm -w packages/cross-seed run build && node --watch packages/cross-seed/dist/cmd.js daemon",
		"dev:frontend": "npm -w packages/webui run dev",
		"seed-test-data": "node scripts/seed-test-data.js",
		"prepare": "node -e \"const fs=require('fs'); if(!fs.existsSync('.git')) process.exit(0); require('child_process').execSync('husky',{stdio:'inherit'})\"",
		"lint": "npm --workspaces run lint --if-present",
		"typecheck": "npm --workspaces run typecheck --if-present",
		"format": "prettier --write . --ignore-path .gitignore",
		"format:check": "prettier --check . --ignore-path .gitignore",
		"release": "node scripts/release.js --"
	},
	"devDependencies": {
		"@types/bcryptjs": "^2.4.6",
		"@types/bencode": "^2.0.1",
		"@types/ms": "^2.1.0",
		"@types/node": "^24.12.0",
		"@types/xml2js": "^0.4.9",
		"@types/xmlrpc": "^1.3.6",
		"@typescript-eslint/eslint-plugin": "^8.57.1",
		"@typescript-eslint/parser": "^8.57.1",
		"@vitest/coverage-v8": "^4.1.0",
		"bencode": "^4.0.0",
		"concurrently": "^9.1.2",
		"eslint": "^8.56.0",
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0",
		"prettier": "^3.8.1",
		"prettier-plugin-tailwindcss": "^0.7.2",
		"rimraf": "^6.1.3",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	},
	"prettier": {
		"tabWidth": 4,
		"useTabs": true,
		"proseWrap": "always"
	},
	"lint-staged": {
		"*.{js,ts}": "eslint --cache",
		"*.{js,json,ts,css,md}": "prettier --write"
	}
}
